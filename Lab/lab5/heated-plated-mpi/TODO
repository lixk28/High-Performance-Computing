
1. 设置 W 边界的值
2. 计算边界的均值 mean
3. 设置 W 内部的值为 mean
4. 开始迭代，首先保存 w 为 u，w 更新为上下左右相邻四个的平均值，
   每次迭代计算最大的相对误差作为 diff，TOL 设置为 epsilon，直到 diff < epsilon

MPI 并行思路：
- 初始化部分不好结构化地并行（1、2、3），且开销不值得，沿用 openmp 版本的代码，放在 master 进程中执行
- 需要发送给 slave 的数据：w（广播）
- 需要由 master 接收的数据：my_w（w 的部分行）, my_diff（由 master 判断是否终止迭代，用 MPI_Reduce 实现）
- 然后 master 用 my_w 更新 w，再把 w 广播给各个进程
- 统计时间，打印数据
- 注意处理任务不能完美平均分配的情况